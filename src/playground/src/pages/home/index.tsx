import { Button } from "@mui/material";

export default () => {
    return(
        <div>
            <Button variant="contained" disableElevation disableRipple>Acessar</Button>
        </div>
    );
}